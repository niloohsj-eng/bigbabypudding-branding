# photos_in/

Drop raw phone photos here, then run `python3 build_post.py photos_in/your_photo.jpg --bg brown` (or `--bg cream`) from the repo root.

The test_character.png file is just a sanity check used during setup. Feel free to delete it.
